很棒的学习计划！GLib题目可以从基础数据结构、字符串处理、内存管理、事件循环、线程、IO等方面逐步深入。下面为你整理100道GLib练习题，涵盖从入门到进阶，便于你系统性练习（建议本地编写和运行代码）。

---

### 基础数据结构（GList、GSList、GArray、GHashTable、GTree、GQueue）

1. 使用GList实现一个整数链表，插入10个元素并遍历输出。
2. 用GSList实现字符串链表，添加、删除、查找指定字符串。
3. 用GArray实现动态数组，存储浮点数并求平均值。
4. 用GArray实现字符串数组，插入、删除、查找操作。
5. 用GHashTable实现学生学号-姓名的映射，支持增删查。
6. 用GHashTable实现单词计数器，统计一段文本中每个单词出现次数。
7. 用GTree实现整数排序二叉树，插入并中序遍历输出。
8. 用GQueue实现先进先出队列，模拟任务调度。
9. 用GList实现链表反转。
10. 用GHashTable实现LRU缓存的核心数据结构。

---

### 字符串与内存管理

11. 使用g_strdup复制字符串并释放内存。
12. 用g_strsplit分割字符串，统计分割后子串数量。
13. 用g_strjoin连接字符串数组为一个字符串。
14. 用g_ascii_strcasecmp实现不区分大小写的字符串比较。
15. 用g_str_has_prefix/g_str_has_suffix判断字符串前后缀。
16. 用g_strstrip去除字符串首尾空白。
17. 用g_memdup复制一段内存并验证内容一致。
18. 用g_malloc/g_free实现动态内存分配和释放。
19. 用g_strdup_printf格式化字符串。
20. 用g_strreverse反转字符串。

---

### 错误处理与日志

21. 用GError实现错误传递与处理。
22. 用g_warning/g_message/g_error输出日志信息。
23. 用g_assert断言检查变量值。
24. 用g_return_val_if_fail实现参数校验。
25. 用g_log自定义日志输出格式。

---

### 事件循环与定时器

26. 用GMainLoop实现一个简单的事件循环。
27. 用g_timeout_add注册定时器，每秒输出一次时间。
28. 用g_idle_add注册空闲回调，处理后台任务。
29. 用GSource自定义事件源。
30. 用GMainContext实现多事件循环。

---

### 线程与同步

31. 用GThread创建新线程，在线程中输出信息。
32. 用GMutex实现线程安全的计数器。
33. 用GCond实现生产者-消费者模型。
34. 用GAsyncQueue实现线程间消息队列。
35. 用g_thread_pool_new实现线程池任务分发。
36. 用g_once实现只执行一次的初始化。
37. 用g_atomic_int_add实现原子加操作。
38. 用g_rec_mutex实现递归互斥锁。
39. 用g_rw_lock实现读写锁。
40. 用g_thread_join等待线程结束。

---

### 文件与IO

41. 用g_file_get_contents读取文件内容。
42. 用g_file_set_contents写入文本到文件。
43. 用g_mkdir创建目录。
44. 用g_remove删除文件。
45. 用g_rename重命名文件。
46. 用g_file_test判断文件是否存在。
47. 用GIOChannel读取标准输入并输出。
48. 用GKeyFile解析ini配置文件。
49. 用g_dir_open遍历目录下所有文件。
50. 用g_file_read_link读取符号链接目标。

---

### 时间与日期

51. 用g_get_current_time获取当前时间。
52. 用g_timer_new测量代码执行时间。
53. 用g_usleep实现延时。
54. 用g_date_new_dmy创建日期对象。
55. 用g_date_strftime格式化日期字符串。

---

### 进阶数据结构与算法

56. 用GHashTable实现多值映射（key对应多个value）。
57. 用GTree实现自定义排序规则。
58. 用GList实现链表排序。
59. 用GArray实现二分查找。
60. 用GQueue实现循环队列。

---

### GObject基础

61. 定义一个简单的GObject类，包含一个整型属性。
62. 实现GObject的信号机制，触发自定义信号。
63. 用GObject实现继承和多态。
64. 用GObject实现属性通知。
65. 用GObject实现对象引用计数。

---

### GIO与异步IO

66. 用GFile读取本地文件内容。
67. 用GFileMonitor监控文件变化。
68. 用GSocket实现TCP客户端。
69. 用GSocketService实现TCP服务器。
70. 用GResolver进行DNS解析。

---

### 国际化与本地化

71. 用g_locale_to_utf8实现本地编码到UTF-8转换。
72. 用g_get_language_names获取系统语言列表。
73. 用g_get_user_name获取当前用户名。
74. 用g_get_home_dir获取用户主目录。
75. 用g_get_tmp_dir获取临时目录。

---

### 杂项与实用工具

76. 用g_random_int生成随机数。
77. 用g_checksum_new计算MD5/SHA1哈希。
78. 用g_base64_encode/g_base64_decode实现Base64编解码。
79. 用g_shell_parse_argv解析命令行参数。
80. 用g_spawn_async启动子进程。

---

### 进阶应用与综合练习

81. 用GList实现约瑟夫环问题。
82. 用GHashTable实现简单的DNS缓存。
83. 用GThread和GAsyncQueue实现多线程日志系统。
84. 用GTimer和GArray实现排序算法性能对比。
85. 用GKeyFile实现配置文件热加载。
86. 用GFileMonitor实现目录同步工具。
87. 用GSocket实现聊天室客户端。
88. 用GTree实现词频统计并按频率排序。
89. 用GObject实现插件系统的基础框架。
90. 用GThreadPool实现并发文件下载。

---

### 挑战题与项目实践

91. 用GLib实现一个简易的HTTP服务器（支持GET）。
92. 用GHashTable和GList实现LRU缓存。
93. 用GIO实现异步文件拷贝工具。
94. 用GObject和GSignal实现事件驱动的任务调度器。
95. 用GThread和GAsyncQueue实现多线程爬虫。
96. 用GKeyFile和GFileMonitor实现配置热更新的服务进程。
97. 用GSocket和GThread实现高并发TCP服务器。
98. 用GChecksum实现文件完整性校验工具。
99. 用GTree和GList实现自动补全功能。
100. 用GLib实现一个支持插件的命令行工具框架。

---

如需某一题的详细题目描述或参考代码，欢迎随时提问！